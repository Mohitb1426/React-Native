import React from 'react';
import { Text, View } from 'react-native';

const HeaderWithBackIcon = () => {
  return (
    <View>
      <Text>HeaderWithBackIcon</Text>
    </View>
  );
};

export default HeaderWithBackIcon;
